# include <stdio.h>
# include "main.h"
/**
 * _isupper: To check if the subject is uppercase or lowercase
 * @c: Parameter for subject
 * Return: ALways 0.
 */
int _isupper(int c)
{
	if(c = 'A' && c <= 'Z')
		return (0);
	else
		return (1);
}
