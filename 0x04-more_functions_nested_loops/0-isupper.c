# include <stdio.h>
# include "main.h"
/**
 * main - Entry point
 * _isupper - To check if the subject is uppercase or lowercase
 * @c: Parameter for subject
 * Return: Always 0.
 */
int main(void)
{
	return (0);
}

int _isupper(int c)
{
	if (c = 'A' && c <= 'Z')
		return (0);
	else
		return (1);
}
